# agent-pmo:0b21609
---
# PHASE 2 (FULL BUILD) + PHASE 3 (CACHE). [DIST-CI-LAYOUT]
#
# Everything the pipeline needs, built for Linux and Windows ONCE and published.
# The three jobs below are SIBLINGS: no `needs:` between them, so the focused
# macOS debugger check cannot delay the other platform builds.
#
# That is the fix for the pipeline's largest structural defect. The Windows
# build used to live in the TEST phase, gated on the Ubuntu build - which it
# consumes nothing from - so it began 6m45 after the run started and every
# Windows test chunk began 11m02 after that. 18 minutes of a 41-minute pipeline
# was builds waiting on other builds.
#
# Linux and Windows run the SAME composite action (../actions/build-platform).
# macOS checks only the native debugger build that those platforms cannot test.
#
# PHASE 3 is the handoff boundary. Every artifact a test job needs is uploaded
# here, and NOTHING downstream of this workflow is permitted to compile
# anything. A test leg that invokes cargo, dotnet build or tsc is a bug.
name: CI / Build
'on':
  workflow_call:
    outputs:
      linux-chunks:
        description: JSON array of VS Code feature chunks to run on Ubuntu.
        value: ${{ jobs.build-linux.outputs.chunks }}
      win-chunks:
        description: JSON array of VS Code feature chunks to run on Windows.
        value: ${{ jobs.build-windows.outputs.chunks }}
env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
permissions:
  contents: read
jobs:
  build-linux:
    name: Linux
    runs-on: ${{ vars.UBUNTU_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    outputs:
      chunks: ${{ steps.build.outputs.chunks }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build every Linux artifact
        id: build
        uses: ./.github/actions/build-platform
        with:
          platform: linux
          vsix-platform: linux-x64
          host-binary: target/release/sharplsp
          suite-artifact: vsix-suite-linux
          binaries-artifact: linux-lsp-binaries
          debugger-artifact: linux-netcoredbg
          # One platform is enough to catch a layout regression in the
          # standalone archive and the package-manager manifests.
          archive: 'true'

  build-windows:
    name: Windows
    # NO `needs:`. This is a sibling of build-linux, not its successor - it
    # consumes nothing Ubuntu produces, and gating it on Ubuntu bought the
    # pipeline nothing but 6m45 of idle Windows runner.
    runs-on: ${{ vars.WINDOWS_RUNNER || 'windows-latest' }}
    timeout-minutes: 40
    outputs:
      chunks: ${{ steps.build.outputs.chunks }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build every Windows artifact
        id: build
        uses: ./.github/actions/build-platform
        with:
          platform: win
          vsix-platform: win32-x64
          host-binary: target/release/sharplsp.exe
          suite-artifact: vsix-suite-win
          binaries-artifact: win-lsp-binaries
          debugger-artifact: win-netcoredbg

  build-macos:
    name: macOS debugger build check
    # [DIST-CI-SMOKE] [DIST-DEBUGGER-BUNDLE] Linux/Windows cannot catch a
    # macOS clang or linker failure in the patched debugger before release.
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10.0.303
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '20'
      - name: Build the patched macOS debugger once
        shell: bash
        run: bash tools/vsix/build-netcoredbg.sh darwin-arm64 --rebuild
      - name: Verify the built debugger runs
        shell: bash
        run: |-
          set -euo pipefail
          test -f target/netcoredbg/darwin-arm64/netcoredbg/ManagedPart.dll
          target/netcoredbg/darwin-arm64/netcoredbg/netcoredbg --version
